import { Toolbox } from '../services/toobox';

export class Pick {

    constructor(
        public toolbox: Toolbox,
    ) {
        // this.toolbox.assetsManager.addScript('assets', 'scripts', 'lib', 'pick.js');
    }

}